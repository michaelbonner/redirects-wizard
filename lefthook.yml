pre-commit:
  parallel: true
  jobs:
    - name: prettier
      glob: "{*,**/*}.{svelte,ts,js,css,json,md,yml,yaml}"
      run: bunx prettier --write --ignore-unknown {staged_files}
      stage_fixed: true
